const appFunctions = document.addEventListener('DOMContentLoaded', () => {
  const model = {};

  const controller = {
    init() {},
  };

  const view = {
    init() {},
    render() {},
  };

  controller.init();
});

export { appFunctions };
